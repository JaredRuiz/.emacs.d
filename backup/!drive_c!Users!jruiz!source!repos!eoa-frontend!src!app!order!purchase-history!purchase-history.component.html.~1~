<div class="Purchase-History-Container">
  <label>Purchase history</label>

  <!-- TODO header -->

  
  <ng-container *ngFor="let historyItem of equipment.purchaseHistory">
    <div class="Purchase-History-Row">

      <div class="History-Item-Data">
        <span>{{ historyItem.purchaseOrderNumber }}</span>
      </div>

      <div class="History-Item-Data">
        <span>{{ historyItem.status }}</span>
      </div>
      
      <div class="History-Quantity-Update">
        <span>{{ historyItem.previousQuantity }}</span>
        <span>{{ historyItem.currentQuantity }}</span>
      </div>

      <div class="History-Item-Data">
        <span>{{ historyItem.changeDate }}</span>
      </div>

      <div class="History-Item-Data">
        <span>{{ historyItem.revisionBulletinNumber }}</span>
      </div>

      <div class="History-Item-Data">
        <span>{{ historyItem.comments }}</span>
      </div>

                                    <div class="History-Item-Data">
        <span>{{ historyItem.purchaseOrderNumber }}</span>
      </div>

      
    </div>

  </ng-container>
</div>
