export interface EquipmentCardModel {
    selected: boolean;
    disabled: boolean;
    expanded: boolean;

    // TODO: we have an any in the component, so this stuff is not coming up...this is wrong!
}
